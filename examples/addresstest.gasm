.org %0200
.word #00

lda #0F
jsr ~DELAY

.org %FFFC
.word #02
.word #00
.org
// busy loops for however many iterations the accumulator has in it
sr DELAY:
    cpa 02
    lda #00
    cpa 00
    lda #01
    cpa 01
    LOOP:
        add 00, 01
        cpa 00
        bne 02, ~LOOP
    rts DELAY